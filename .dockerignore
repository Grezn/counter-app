# Git 歷史和 CI 設定不需要放進 runtime image。
.git
.github

# MCP / Skills 是本機與 agent 開發工具，不需要放進 runtime image。
.mcp.example.json
mcp/
skills/

# 本機 dependencies/logs 不要複製進 image，dependencies 會在 image 裡重新安裝。
node_modules
npm-debug.log*

# 不要把秘密設定檔複製進 Docker image。
.env
.env.*
**/.env
**/.env.*
!.env.example
!**/.env.example

# Terraform 是基礎設施設定，不是 Node.js container 需要的檔案。
infra/
**/.terraform
**/*.tfstate
**/*.tfstate.*
